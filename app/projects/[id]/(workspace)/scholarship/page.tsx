import { PhaseNote } from "@/components/ui/PhaseNote";

export const dynamic = "force-dynamic";

export default function ScholarshipPage() {
  return (
    <PhaseNote phase="7">
      The SQUIRE 2.0 drafter, the abstract formatter, the IRB determination pre-check and the meeting matcher live
      here. They draft from this project&rsquo;s record, PDSA log and charts, and never invent a result.
    </PhaseNote>
  );
}
